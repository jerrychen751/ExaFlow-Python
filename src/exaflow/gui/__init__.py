"""
GUI package for the 3D Navier-Stokes solver. Separated from core solver code, so the solver modules import no UI dependency, except the streaming client a run sends its states to the viewer through.
"""

